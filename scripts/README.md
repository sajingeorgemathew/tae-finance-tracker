# scripts

Operational, developer-run scripts: workbook import, Excel backup/export,
Supabase type generation.

Still empty after FINANCE-DATA-01. The one command worth knowing lives in
`docs/FINANCE-DATA-01.md` ("Generated database types") and is run by hand, not
checked in as a script yet.

Anything added here that touches `reference/` must open it read-only.
