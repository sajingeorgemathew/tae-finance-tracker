# types

Shared TypeScript types: database row types generated from Supabase, plus
domain types shared across `lib/` and `components/`.

Empty in FINANCE-BOOTSTRAP-00 — there is no schema to generate from yet.
