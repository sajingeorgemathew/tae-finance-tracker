# lib/excel

Workbook import (AS-IS, no normalisation) and Excel export/backup, using `xlsx`.

Empty in FINANCE-BOOTSTRAP-00. Import must read
`reference/finance-tracker.xlsx.xlsx` read-only and never write back to it.
