# lib/email

Resend transport and send-tracking for receipts and reminders.

Empty in FINANCE-BOOTSTRAP-00. `resend` is installed but deliberately never
imported, configured or called — no mail can be sent from this codebase yet.
