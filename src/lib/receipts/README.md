# lib/receipts

Receipt generation with `@react-pdf/renderer`, receipt numbering and
sent/resend tracking.

Empty in FINANCE-BOOTSTRAP-00. The layout to match is
`reference/receipt-template.pdf.pdf` — read-only.
