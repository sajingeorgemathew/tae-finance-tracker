# components/finance

Finance tracker UI: the virtualised Excel-style grid (`@tanstack/react-table` +
`@tanstack/react-virtual`), filters and batch actions.

Currently contains `tracker-placeholder.tsx` only — the real grid arrives with
the finance schema.
