# components/receipts

React components for receipt preview, receipt status badges and the
send/resend UI. Empty until the receipts ticket.

PDF rendering itself lives in `src/lib/receipts`, not here.
